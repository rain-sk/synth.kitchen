{
  "name": "synth.kitchen-api",
  "version": "0.5.10",
  "devDependencies": {
    "concurrently": "9.2.1",
    "cypress": "15.5.0",
    "typescript": "5.9.3"
  },
  "scripts": {
    "dev": "npm i --no-audit && concurrently -k -n shared,api,web 'cd app/shared && npm run dev' 'cd app/api && npm run dev' 'cd app/web && npm run dev'",
    "lint": "npm i --no-audit && concurrently -k -n shared,api,web 'cd app/shared && npm run lint' 'cd app/api && npm run lint' 'cd app/web && npm run lint'",
    "test": "npx cypress run",
    "prepare": "concurrently -n shared,api,web 'cd app/shared && npm i --no-audit' 'cd app/api && npm i --no-audit' 'cd app/web && npm i --no-audit'",
    "build:prod": "npm i --no-audit && cd app/shared && npm run build && cd ../api && npm run build && cd ../web && npm run build"
  }
}
