export type DELAY_STATE_VERSIONS = ["0.5.0"];
export const DELAY_STATE_VERSIONS: DELAY_STATE_VERSIONS = ["0.5.0"];

export type DELAY_STATE = {
  ["0.5.0"]: {
    version: "0.5.0";
    delayTime: number;
  };
};
