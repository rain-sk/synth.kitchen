export type GAIN_STATE_VERSIONS = ["0.5.0"];
export const GAIN_STATE_VERSIONS: GAIN_STATE_VERSIONS = ["0.5.0"];

export type GAIN_STATE = {
  ["0.5.0"]: {
    version: "0.5.0";
    gain: number;
  };
};
