export type SHIFT_STATE_VERSIONS = ["0.5.0"];
export const SHIFT_STATE_VERSIONS: SHIFT_STATE_VERSIONS = ["0.5.0"];

export type SHIFT_STATE = {
  ["0.5.0"]: {
    version: "0.5.0";
    inputMin: number;
    inputMax: number;
    outputMin: number;
    outputMax: number;
  };
};
