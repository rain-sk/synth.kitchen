export type GATE_STATE_VERSIONS = ["0.5.0"];
export const GATE_STATE_VERSIONS: GATE_STATE_VERSIONS = ["0.5.0"];

export type GATE_STATE = {
  ["0.5.0"]: {
    version: "0.5.0";
    gate: number;
  };
};
