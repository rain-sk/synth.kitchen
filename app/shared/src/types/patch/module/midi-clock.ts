export type MIDI_CLOCK_STATE_VERSIONS = ["0.5.0"];
export const MIDI_CLOCK_STATE_VERSIONS: MIDI_CLOCK_STATE_VERSIONS = ["0.5.0"];

export type MIDI_CLOCK_STATE = {
  ["0.5.0"]: {
    version: "0.5.0";
    input: string;
  };
};
