# Patch Searching

There needs to be a way to quickly get around a larger patch. I could imagine named clusters that are grouped together (like how objects can be grouped in an image editing tool).

There should be a way to search for named groups/modules.
