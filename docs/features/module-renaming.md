# Renaming Modules

It used to be possible to rename modules. I removed this feature but I want to add it back.

First step: find the git commit(s) that removed the module renaming feature, and revert them.
